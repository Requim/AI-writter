# 自动创作实测记录

## 验收目标

- 同一作品连续自动生成并归档 10 章。
- 已保存草稿可恢复；不以绕过字数、事实、目标验收证明完成。

## 已验证

- 作品 ac8c194a-c9c0-4cdf-a76c-3dc3ffe0dae1。
- 第 1 章已归档 5,696 字，自动进入第 2 章，无需逐步确认。
- 归档守卫参数缺失已由 08900e5 修复并在线验证。

## 新问题与证据

- 第 2 章检查点 revision_attempts=5，审读重试=2。
- 最新失败为审读结果不可用后重试耗尽，不是后台节点超时。
- 线上日志连续报告 tactical_fulfillment 缺失。长章汇总模板缺字段，但服务端 schema 要求该字段。
- 定向全文修订分支丢弃具体 reflection_issues，只保留笼统修订指令。
- 浏览器正文区出现目标验收 JSON；修订调用复用了含审读输出要求的目标提示。
- 上轮 bddbdbe 高分归档兜底未检查字数有效性，不足以证明通过，撤销该兜底。

## 修复与边界

- 补齐汇总模板战术报告；运行时附加完整输出 schema，兼容既存模板快照。
- 定向全文修订带上尚未解决的问题与原文证据。
- 修订系统指令明确只输出正文，不输出或复制验收 JSON。
- 保留真实审读及修订上限，不伪造目标通过，不直接改写生产检查点。
- 10 章验收尚未完成。第 2 章最新实测仍停在质量报告阶段。

## 本轮发布

- 1df5d9b：修复汇总审读输出契约、修订反馈丢失，撤销不完整的高分归档兜底。
- af4ef8d：显式质量重试开启新的有限预算，累计 revision_attempts 和历史不清零；普通恢复不重置预算。
- 干净工作副本：100 项相关测试通过，相关 mypy、ruff 通过。
- 生产镜像 fix-af4ef8d 健康；沿用生产既有代码，只应用本次补丁，无数据库迁移。
- 线上旧失败草稿已重新进入 reflection_node；检查点保留 quality_revision_baseline=5。
- 等待本次审读实际结果；不将容器健康、单测通过等同于 10 章验收通过。

## 后续实测与发布

- 296f36d：审读报告声明目标完成但引用不匹配原文时，先进行一次有界引用复核，避免直接触发全文重写。无法提供有效原文证据时仍由原目标门禁拒绝，不伪造通过。
- 干净工作副本 105 项相关测试通过；新增引用复核模块及测试 ruff 通过，相关 mypy 通过。
- 生产镜像 fix-296f36d 已部署，容器健康；未执行数据库迁移。
- 关闭全部测试网页后，后台累计修订次数继续从 6 增长到 8，证明后台执行不依赖网页连接。
- 修订输出约束上线后，第 2 章草稿由含审读 JSON 的 9,066 字变为 6,190 字正文，后续检查未再发现该审读尾部。
- 最新观察：第 2 章累计修订次数 9，显式重试基线仍为 5，历史未清零；当前草稿 5,230 字，正在审读，检查点无执行错误。
- 最新界面仍显示上一轮质量报告；“需要人工审阅”文字与后台正在自动审读并存，可能误导用户认为需要手动继续，记录为待核对的状态展示问题。
- 当前验收仍为 1/10 章已归档，第 2 章审读中；引用复核补丁的完整线上归档结果尚待验证。

## 恢复调度计数修复

- 后续审读停止，检查点实际累计修订 9、基线 5，即本轮完成 4 次，但调度计数为 5，提前触发质量修复上限。
- 质量修订预算改为根据已完成次数计算；重复调度不会额外占用额度。事实修复计数、显式重试基线和累计历史保持不变。
- 新回归覆盖“累计 9、基线 5、调度计数 5”重放三次仍可调度，累计完成到 10 后必须停止。
- 106 项相关测试、相关 mypy 和 ruff 通过；未放宽质量或目标验收。
- 68d3dab 已推送，生产容器健康。显式重试后累计修订由 9 增至 11，自动审读及修订无需逐步确认。

## 手动超时配置遗漏

- 直接读取生产有效设置发现手动节点仍为 240 秒、手动整轮 600 秒，后台节点为 900 秒，与此前用户要求不符。
- 修正源码默认值为手动节点 900 秒、手动整轮 1800 秒，部署时同时覆盖旧环境值，防止外层先于节点截止。
- 加入默认配置回归，与后台超时及前述质量回归合计 116 项测试通过。
- 075909d 已推送并部署，生产容器健康；直接读取有效配置确认手动/后台节点均为 900 秒、手动整轮 1800 秒、后台整轮 86400 秒。
- 发布前检查点累计修订 13、重试基线 9、质量调度计数 4，计数与完成次数一致；草稿 5,212 字且不含审读 JSON 尾部。
- 本轮仍见字数无效及目标 ch2:must:3 的原文证据未被门禁接受，引用复核并未消除所有失败，不宣称已彻底修复生成质量循环。
- 发布后通过网页继续入口恢复已保存的第 2 章审读。最终 10 章验收未完成，已归档仍为 1 章。

## 持续验收：审读口径与修订反馈

- 当前目标保持 10 章归档，未完成前持续诊断修复。
- 第 2 章完成本轮 5 次修订后停止，预算计数正确。最新报告评分 4.7/5，仍存在附页可见性的一致性问题。
- 实际审读将整书均值 4200 字的上下 10% 当作每章硬限制，但服务端目标契约明定分章目标仅作参考，整书才有容差；该冲突造成不必要的全文改写。
- 运行时明确审读口径，兼容已有提示词快照；参考值不当硬限制，真实低密度、遗漏和用户硬限制仍须验收。
- 全文修订此前仅收到问题列表，缺少字数、计划兑现和未通过目标等 gate 字段；补充真实阻塞依据，避免围绕无关风格反复润色。
- 新增口径与反馈回归，119 项相关测试通过。
- 290fa5a 已推送部署，从已保存草稿再次审读。
- 进一步发现修订范围规则重复且矛盾：revision_prompts 将一致性问题归为 patch，reflection 的硬错误分类却将其一律归为 refactor。统一复用既有 classify_revision_mode；hard_failures 仍保留，问题修好前不可归档。
- 新回归验证局部一致性 must_fix 走 patch、逻辑问题和低分稿走 refactor；137 项相关测试通过。
- 线上第 2 章已通过真实审读并归档，检查点 current_chapter_index=2，正文 5,028 字，goal_acceptance 全部通过。自动进入第 3 章，无人工接受质量报告。
- 跨章检查发现累计 revision_attempts=14 保留但下一章预算默认基线为 0，会提前耗尽。新增跨章独立基线，保留累计历史；旧无章节元数据的耗尽状态仍不可绕过。
- 覆盖先发生事实恢复再发生质量修订的跨章顺序，138 项相关测试通过。
- 后续确认细纲节点会将 revision_attempts 归零：这不是永远累计的计数。bc34fe4 将基线同步限制在当前计数以内，避免跨章后沿用过高基线；139 项回归通过。
- 主工作区额外执行目标契约、预算及审读口径集成回归，36 项通过。
- bc34fe4 已推送部署，容器健康。第 3 章已归档 4,061 字；页面显示 3/10 章、累计 14,785 字，自动进入第 4 章。
- 发布中断后通过正常继续入口恢复第 4 章细纲，未改写生产检查点或跳过审读。
- 第 4 章审读成功发现并自动修正提前泄露秘密、离馆动因遗漏和上午/下午矛盾。
- 第二轮仅因引用复核要求 user:summary 的全书摘要在本章连续重现而误判失败。主审原有范围说明正确，但独立引用复核遗漏范围约束；补齐全书兼容性与本章必达事件的区分。
- 同时修正轻微计划偏差解除时覆盖基础质量失败的风险，缺字数报告、低密度、低评分或字数无效均不得直接归档。142 项回归通过。
- 1add8fe 已推送部署且健康；第 4 章重新审读全部目标通过，归档 4,971 字，自动启动第 5 章，无逐步确认。
- 第 5 章正文首次检查发现末尾混入 goal_checks JSON。根因是初稿/场景续写共用目标提示，但只有修订节点追加了正文输出边界。
- 在统一流式正文入口为 chapter_writer_node 与 revision_node 追加“只输出小说正文”约束，覆盖整章初稿、场景续写和全文修订；不改变后续独立审读的 JSON 契约。
- 146 项相关测试通过。第 5 章当前已通过审读并进入故事状态更新，数据库归档待本轮结束后核对。
- 复核发现第 5 章审读未拦截末尾报告，实际归档包含 JSON，因此不计为合格完成。新增结构化尾部识别：只分离末尾完整 goal_checks 对象，保留普通故事 JSON、报告后的叙述、不完整片段及纯报告异常，防止广泛文本截断。
- 将通过正常 AI 重写入口重新生成并独立审读第 5 章，保留后续章节，不直接修改生产数据库。
