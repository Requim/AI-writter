# 后台自动审核发布

日期：2026-09-17

## 范围

- 在现有后台任务及独立超时预算上，自动模式接管暂停的审核节点。
- 简介、规划、质量和事实审核由服务端推进；修复次数写入 checkpoint。
- 事实或目标未通过时修订或重新取证，预算耗尽保留草稿并报错，不伪造人工确认。
- 前端恢复审核现场时请求服务端重试，不直接代替用户接受提案。
- 保留人工模式、主动停止、单节点截止与同步接口原有时限。
- 不保证服务进程重启后自动调度；不包含自主作者等其他工作区改动。

## 验证

- 隔离 Git 基线：后端 473 passed / 60 skipped / 1 warning。
- 前端 139 tests passed；构建与 lint 通过。
- 覆盖人工暂停转自动后完成三章、断开网页后任务继续、事实修复预算耗尽但不归档。
- 生产采用当前镜像加本次补丁，保留已有历史功能；前端历史基线已重建并逐文件匹配上次发布包。
- 发布前备份数据库、检查活跃任务；保留原镜像及完整 Compose 配置链用于健康检查失败回滚。
- 本文记录发布前验证，不代表已部署、真实模型长时运行或数据库恢复演练通过。
