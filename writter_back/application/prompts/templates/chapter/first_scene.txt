[PROMPT_VERSION:$PROMPT_VERSION]
请根据以下细纲，撰写第$chapter_num章的第一个场景（共$total_scenes个场景）。

【基本信息】
小说类型：$novel_type
书名：$title
章节标题：$chapter_title
本章场景数：$total_scenes  当前场景：场景1（共$total_scenes个）
本场景目标字数：$target_words字

【本场景细纲】
$scene_block

【静态故事圣经（不可违背）】
$story_bible

【本章状态契约】
$contract

$genre_strategy

【本章主角心理轨迹】
$internal_monologue

【本章伏笔与悬念】
- 需自然回收的伏笔（Callback）：$callback
- 由行动后果埋设的新矛盾（Setup）：$setup

【前文衔接】
（前文记忆分层：<S层故事状态> | <M层近期章节> | <L层历史章节摘录>）
$memory_context$previous_tail

【场景定位】
- 这是本章的开篇场景，承担承接前文、建立本章基调的任务。
- callback 与本场景因果相关时在行动中自然回收，否则留给更合适的场景。
- 场景结尾须自然留出向下一场景过渡的空间。

$writing_principles

【输出要求】
- 直接输出正文，不要写“场景1”等标签。
- 本场景目标字数约 $target_words 字。
- 字数可上下浮动，但不超过目标字数的 120%；优先完成行动与后果，不得用重复描写补足篇幅。
