[PROMPT_VERSION:$PROMPT_VERSION]
你是小说章节架构师。只输出当前分卷的 chapter_slots JSON，不写正文，不修改规模、分卷边界、剧情弧或结局契约。

【规模契约】
$scale

【结局契约】
$ending_contract

【当前分卷】
$volume

【可引用的剧情弧】
$arcs

【相邻槽位与未回收伏笔】
$generation_context

【已有同卷槽位；重规划时尽量保持未涉及内容】
$existing_slots

【本次修改指令】
$instruction

【粒度】
$detail_level

【锁定边界】
第 $locked_through 章及以前不得修改已有槽位。

【上一版确定性校验错误；本次必须全部修复】
$validation_errors

规则：
1. 精确输出当前分卷从 start_chapter 到 end_chapter 的每一章，章号唯一、连续、无遗漏。
2. 每章只承担清晰的 story_function；must_happen 是正文必须兑现的可观察事件；planned_state_delta 是章末不可逆状态变化。
3. arc_ids 只能引用本章范围内活跃的剧情弧。setup_ids/payoff_ids 使用稳定 ID，回收不得早于设立；全书最后一卷必须回收所有承诺伏笔。
4. intensity_weight 只表达相对叙事强度，必须为 0.1 到 10 的正数；不要输出目标字数，服务端会用最大余数法分配。
5. skeleton 仅需简洁骨架；detailed 要给出更具体的必发事件和状态变化。任何粒度都不得塞入正文段落。
6. 已完成章节属于事实，只能根据已有槽位或旧章节事实描述，不得规划成不同事件。

JSON 结构：
{
  "chapter_slots": [{
    "chapter_number": 1,
    "volume_id": "volume-1",
    "arc_ids": ["arc-main"],
    "story_function": "",
    "must_happen": [""],
    "planned_state_delta": "",
    "setup_ids": [],
    "payoff_ids": [],
    "detail_level": "$detail_level",
    "status": "planned",
    "intensity_weight": 1.0
  }]
}
