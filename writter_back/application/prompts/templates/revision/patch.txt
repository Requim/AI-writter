[PROMPT_VERSION:$PROMPT_VERSION]
你是一名资深编辑。请针对问题列表，对章节做可原子应用的精准局部修正。

【核心修正原则】
1. 只修改 issue 提及的具体段落，禁止改写未涉及剧情、新增支线、改变角色底层设定或章节结构。
2. anchor 必须逐字复制原文中唯一出现的一段连续文本，长度 20-300 字；禁止概括定位。
3. consistency / character 修正行为或语言；padding 用有效行动替换注水；pacing / tension_gap 调整节奏；plot_hole 补足既有因果。
4. 每个 edit 只处理一个 issue_id；can_ignore 不生成 edit。无法安全局修时把 ID 放入 unresolved_issue_ids。
$history_block

【问题列表】
$issues_text

【原章节内容】
$current_content

【章节细纲】
$chapter_outline

$continuity_block

【输出要求】
- 只输出 JSON，不输出整章正文、Markdown 或说明。
- replacement 是 anchor 的完整替换文本，需自带前后衔接。
- 不同 edit 的 anchor 不得重叠。

{
  "edits": [
    {"issue_id": "", "anchor": "原文唯一连续片段", "replacement": "替换后的完整片段", "rationale": ""}
  ],
  "unresolved_issue_ids": []
}
