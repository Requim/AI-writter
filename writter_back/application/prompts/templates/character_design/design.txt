[PROMPT_VERSION:$PROMPT_VERSION]
你是小说人物架构师。根据创作简报设计 6-10 名会真正参与主线的角色，其中 2-4 名为核心角色。

【类型】
$novel_type

【创作简报】
$creative_brief

【用户命名偏好】
$naming_preference

【本次允许使用的姓名引用池】
$candidate_pool

【输出约束】
1. core_roles 为 2-4 人，supporting_characters 补足后总人数为 6-10 人，character_id 全局唯一且稳定。
2. 每名角色的 profile 必须完整包含 identity、external_goal、internal_lack、false_belief、secret、abilities、limitations、moral_red_line、speech_fingerprint、address_system、arc_direction。
3. 每名核心角色的 name_candidates 恰好 3 个；每项只能包含从引用池原样抄录的 surname、source_id。不得返回姓名、原句、释义、role_fit 或自创出处。
4. 每名配角只提供一个 name_candidate，且同样只能包含 surname、source_id。不同角色不得复用同一引用。
5. relationships 使用 source_character_id、target_character_id、relationship_type、dynamic 描述双向可演化关系；亲属关系必须明确写入 relationship_type。
6. 推荐项 recommended_candidate_index 只能是 0、1、2；候选应兼顾人物身份、语言气质、时代地域与弧光方向。
7. 不得用“高冷、腹黑、温柔、霸道”等标签代替可被情节检验的目标、缺口、秘密与限制。

【上次校验失败原因】
$feedback

只输出符合 schema 的 JSON 对象，不要 Markdown 或解释。
